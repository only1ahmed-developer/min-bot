// Point this at your deployed backend (Render/Railway/Heroku). During local
// development this default is fine if the backend runs on port 5000.
//
// IMPORTANT: replace the fallback below with your real Render backend URL
// before deploying to Vercel, e.g. 'https://adevos-min-bot-backend.onrender.com'
window.ADEVOS_API_BASE = window.ADEVOS_API_BASE || 'https://your-backend.onrender.com';

// Telegram bot username shown on landing/dashboard "Open Telegram Bot" buttons.
window.ADEVOS_TELEGRAM_BOT_USERNAME = window.ADEVOS_TELEGRAM_BOT_USERNAME || 'AdevosMinBot';
